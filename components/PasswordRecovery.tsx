// Este componente foi intencionalmente deixado em branco.
// A funcionalidade de recuperação de senha não é aplicável a um sistema de login
// puramente local (offline) que não tem a capacidade de enviar e-mails.
// A remoção da lógica evita confusão para o usuário.

import React from 'react';

const PasswordRecovery: React.FC = () => {
  return null;
};

export default PasswordRecovery;
